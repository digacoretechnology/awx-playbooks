---
- name: Manage Kiosk Configuration
  hosts: all
  

  tasks:
    - name: Update the KIOSK_URL in the kiosk.env file
      ansible.builtin.lineinfile:
        path: /etc/kiosk.env
        regexp: '^KIOSK_URL='
        line: 'KIOSK_URL="{{ https://google.com }}"'